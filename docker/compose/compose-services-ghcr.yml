services:
  auth-service:
    container_name: auth-service
    image: ghcr.io/smile-health/backend/auth-service:demo
    restart: unless-stopped
    env_file:
      - ../env/auth-service.env
    networks:
      - smile-network
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    labels:
      traefik.enable: "true"
      traefik.http.routers.auth-service.rule: "PathPrefix(`/auth`)"
      traefik.http.routers.auth-service.entrypoints: "web"
      traefik.http.services.auth-service.loadbalancer.server.port: "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  core:
    container_name: core
    image: ghcr.io/smile-health/backend/core:demo
    restart: unless-stopped
    env_file:
      - ../env/core.env
    networks:
      - smile-network
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      traefik.enable: "true"
      traefik.http.routers.core.rule: "PathPrefix(`/core`)"
      traefik.http.routers.core.entrypoints: "web"
      traefik.http.services.core.loadbalancer.server.port: "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  main:
    container_name: main
    image: ghcr.io/smile-health/backend/main:demo
    restart: unless-stopped
    env_file:
      - ../env/main.env
    networks:
      - smile-network
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    labels:
      traefik.enable: "true"
      traefik.http.routers.main.rule: "PathPrefix(`/main`)"
      traefik.http.routers.main.entrypoints: "web"
      traefik.http.services.main.loadbalancer.server.port: "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  sync-service:
    container_name: sync-service
    image: ghcr.io/smile-health/backend/sync-service:demo
    restart: unless-stopped
    env_file:
      - ../env/sync-service.env
    networks:
      - smile-network
    depends_on:
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    labels:
      traefik.enable: "true"
      traefik.http.routers.sync-service.rule: "PathPrefix(`/sync`)"
      traefik.http.routers.sync-service.entrypoints: "web"
      traefik.http.services.sync-service.loadbalancer.server.port: "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  warehouse-service:
    container_name: warehouse-service
    image: ghcr.io/smile-health/backend/warehouse-service:demo
    restart: unless-stopped
    env_file:
      - ../env/warehouse-service.env
    networks:
      - smile-network
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      traefik.enable: "true"
      traefik.http.routers.warehouse-service.rule: "PathPrefix(`/warehouse`)"
      traefik.http.routers.warehouse-service.entrypoints: "web"
      traefik.http.services.warehouse-service.loadbalancer.server.port: "3008"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3008/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # notification:
  #   container_name: notification
  #   image: ghcr.io/smile-health/backend/notification:demo
  #   restart: unless-stopped
  #   env_file:
  #     - ../env/notification.env
  #   networks:
  #     - smile-network
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #   labels:
  #     traefik.enable: "true"
  #     traefik.http.routers.notification.rule: "PathPrefix(`/notification`)"
  #     traefik.http.routers.notification.entrypoints: "web"
  #     traefik.http.services.notification.loadbalancer.server.port: "3000"
  #   healthcheck:
  #     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s

networks:
  smile-network:
    external: true
